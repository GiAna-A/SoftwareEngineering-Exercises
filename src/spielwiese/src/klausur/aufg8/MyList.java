package spielwiese.src.klausur.aufg8;

/*
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.function.Consumer;

public class MyList implements Comparable {
    private MyList[] list;

    public static void main(String[] args) {
        List<Integer> gehälter1 = new ArrayList<>();
        List<Double> gehälter2 = new ArrayList<>();
        gehälter1.add(2000);
        gehälter1.add(2);

        gehälter2.add(6500.25);
        gehälter2.add(3.2);

        System.out.println(getSmallestElement());
    }




    public static <T> Comparable<T> getSmallestElement(MyList list) {
        T smallestElement = null;

            GehälterComp  comp= new GehälterComp();
//comp.compare( list.);

        return (Comparable<T>) smallestElement;
    }

    @Override
    public int compareTo(Object o) {
        return 0;
    }
}
*/